# Leave empty for the recommended same-origin production deployment.
# For a separately hosted API, use its full HTTPS base URL, for example:
# VITE_API_BASE_URL=https://api.example.com/api
VITE_API_BASE_URL=
