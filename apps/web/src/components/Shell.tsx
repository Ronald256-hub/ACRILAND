import { NavLink,Outlet } from "react-router-dom";
import { useAuth } from "../auth/AuthContext";

const managementLinks=[["/","Command Centre","dashboard.view"],["/vehicles","Vehicles","vehicle.view"],["/drivers","Drivers","driver.view"],["/assignments","Assignments","assignment.view"],["/trips","Trip Control","trip.view"],["/inspections","Inspections","inspection.view"],["/users","Users","user.view"],["/branches","Branches","branch.view"],["/departments","Departments","department.view"],["/audit","Audit Log","audit.view"]] as const;
const driverLinks=[["/","Driver Home"],["/assignments","My Vehicle"],["/trips","My Trips"],["/inspections","Inspections"],["/my-profile","My Profile"]] as const;
export function Shell(){const{me,logout}=useAuth();const isDriver=me?.roles.includes("DRIVER")??false;return <div className="app-shell"><aside className="sidebar"><div className="brand"><div className="brand-mark">A</div><div><b>ACRILAND</b><span>Fleet Command</span></div></div><nav>{isDriver?driverLinks.map(([to,label])=><NavLink key={to} to={to} end={to==="/"}>{label}</NavLink>):managementLinks.filter(([, ,permission])=>me?.permissions.includes(permission)).map(([to,label])=><NavLink key={to} to={to} end={to==="/"}>{label}</NavLink>)}</nav><div className="sidebar-foot"><div className="user-mini"><b>{me?.fullName}</b><span>{me?.roles.join(" · ")}</span></div><button className="ghost" onClick={()=>void logout()}>Sign out</button></div></aside><main><header className="topbar"><div><span className="eyebrow">ACRILAND LTD</span><h1>{isDriver?"Driver Operations":"Fleet Command Centre"}</h1></div><div className="secure-pill">● Secure session</div></header><div className="page"><Outlet/></div></main></div>}
