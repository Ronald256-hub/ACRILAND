import { useState,type FormEvent } from "react";
import { Link } from "react-router-dom";
import { api } from "../api/client";
export function ForgotPasswordPage(){const[org,setOrg]=useState("acriland"),[email,setEmail]=useState(""),[message,setMessage]=useState(""),[error,setError]=useState("");const submit=async(e:FormEvent)=>{e.preventDefault();try{const r=await api<{message:string}>("/auth/forgot-password",{method:"POST",body:JSON.stringify({organizationSlug:org,email})});setMessage(r.message);setError("");}catch(x){setError(x instanceof Error?x.message:"Unable to request reset")}};return <div className="login-wrap"><section className="login-copy"><span className="eyebrow">Account recovery</span><h1>Recover<br/>secure<br/>access.</h1></section><form className="login-card" onSubmit={submit}><h2>Forgot password</h2><label>Organization<input value={org} onChange={e=>setOrg(e.target.value)}/></label><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label>{message&&<div className="callout">{message}</div>}{error&&<div className="error-box">{error}</div>}<button className="primary">Send reset instructions</button><Link to="/login">Back to sign in</Link></form></div>}
