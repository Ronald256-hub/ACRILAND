import { useState,type FormEvent } from "react";
import { Link,Navigate } from "react-router-dom";
import { useAuth } from "../auth/AuthContext";
export function LoginPage(){const{me,login}=useAuth();const[org,setOrg]=useState("acriland"),[email,setEmail]=useState(""),[password,setPassword]=useState(""),[error,setError]=useState(""),[busy,setBusy]=useState(false);if(me)return <Navigate to="/" replace/>;const submit=async(e:FormEvent)=>{e.preventDefault();setBusy(true);setError("");try{await login(org,email,password);}catch(err){setError(err instanceof Error?err.message:"Sign in failed.");}finally{setBusy(false);}};return <div className="login-wrap"><section className="login-copy"><span className="eyebrow">ACRILAND LTD</span><h1>Every truck.<br/>Every trip.<br/>Accountable.</h1><p>Enterprise command, control, maintenance and compliance for the ACRILAND fleet.</p><div className="control-list"><span>✓ No public driver registration</span><span>✓ Action-level permissions</span><span>✓ Permanent audit trail</span></div></section><form className="login-card" onSubmit={submit}><div className="brand login-brand"><div className="brand-mark">A</div><div><b>Fleet Command</b><span>Authorized personnel only</span></div></div><label>Organization<input value={org} onChange={e=>setOrg(e.target.value)} autoComplete="organization"/></label><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} autoComplete="username" required/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} autoComplete="current-password" required/></label>{error&&<div className="error-box">{error}</div>}<button className="primary" disabled={busy}>{busy?"Signing in…":"Sign in securely"}</button><Link to="/forgot-password">Forgot password?</Link><small>Accounts are issued by authorized ACRILAND management. There is no self-registration.</small></form></div>}
